name=medshared
version=0.1
final.name=${name}
year=2007

basedir = ./
src.dir = ./src
lib.dir = ./lib
jpa.lib.dir=./openjpalib
conf.dir = ./conf

build.dir = ./build
build.classes = ${build.dir}/classes
build.javadoc = ${build.docs}/api
build.encoding = utf-8

dist.dir=${build.dir}/${final.name}

javac.debug=on
javac.optimize=on
javac.deprecation=off
javac.version= 1.5